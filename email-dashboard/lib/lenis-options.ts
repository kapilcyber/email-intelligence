/** Shared options for dashboard Lenis instances (main, sidebar, nested regions). */
export const LENIS_SHARED_OPTS = {
  duration: 1,
  smoothWheel: true,
  wheelMultiplier: 1,
  touchMultiplier: 1.2,
  allowNestedScroll: true,
} as const;
