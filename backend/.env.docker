# Loaded before backend/.env by docker-compose (see docker-compose env_file order).
# Split Ollama: classification vs summarization (override in backend/.env if needed).
OLLAMA_CLASSIFY_BASE_URL=http://172.16.200.30:11434
OLLAMA_SUMMARY_BASE_URL=http://172.16.200.6:11434
